# Dependencies
node_modules/

# Build output
/build/
/.svelte-kit/
/dist/
src-tauri/target/
src-tauri/gen/

# Secrets / local config
.env
.env.local
*.local

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.log
npm-debug.log*

# Tauri signing keys
*.key

# Speech model for on-device subtitles: ~142 MB, fetched by scripts/fetch-whisper-model.mjs.
# The directory itself is tracked (its README keeps the Tauri resource glob matching).
src-tauri/resources/models/*.bin
